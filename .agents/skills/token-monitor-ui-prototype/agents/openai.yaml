interface:
  display_name: "Token Monitor UI Prototype"
  short_description: "在不接入正式入口的前提下比较两至三套 Token Monitor 布局。"
  default_prompt: "Use $token-monitor-ui-prototype to compare isolated Token Monitor dashboard directions."
policy:
  allow_implicit_invocation: true
